@model IEnumerable<Films.FilmGenre>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdfilmNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdgenresNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdfilmNavigation.Descript)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdgenresNavigation.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdfilmGenres">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdfilmGenres">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdfilmGenres">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
